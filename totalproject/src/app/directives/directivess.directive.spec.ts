import { DirectivessDirective } from './directivess.directive';

describe('DirectivessDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivessDirective();
    expect(directive).toBeTruthy();
  });
});
